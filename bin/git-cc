#!/bin/bash
#---
#title: Закоммитить то что есть
#tags: [git]
#refs: []
#---
if [ "$#" -eq 0 ]; then
      echo "Illegal number of parameters"
      exit
fi
DIR=`pwd`
COMMIT="$@"
git commit -m "$COMMIT"
