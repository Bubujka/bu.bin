#!/bin/bash
. ~/.bashrc

TARGET="$(echo '' | dmenu-wrapper 'Журнал' 90)"
if [ ! -n "$TARGET" ]; then
  echo "Aborted" | dzen2-wrapper red ; exit
fi

echo >> ~/.db/wiki/_journal
echo $(date -Is)':' "$TARGET" >> ~/.db/wiki/_journal

echo "Writed" | dzen2-wrapper green &
write-to-journal
