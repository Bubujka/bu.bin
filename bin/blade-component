#!/bin/bash
COMPONENT=$(find resources/views/components -iname '*.blade.php' | sed  's/resources\/views\/components\///' | sed 's/\.blade\.php//' | sed 's/\//./g' | sort | fzf )
if [[ "$1" == '--full' ]]; then
  echo "<x-$COMPONENT>"
  echo "</x-$COMPONENT>"
else
  echo "<x-$COMPONENT />"
fi
